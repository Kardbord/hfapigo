# hfapigo/api

This package holds public request and response [DTOs](https://en.wikipedia.org/wiki/Data_transfer_object)
and shared option types used by the library.

Most users should only need the root `hfapigo` package.
Import `hfapigo/api` if you need to reference request/response structs directly.
