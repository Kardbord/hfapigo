---
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.5%
    patch:
      default:
        target: auto
        threshold: 0.5%

ignore:
  - "examples/**"
  - "internal/testutils/**"
  - "internal/version/**"
  - "version.go"
