meta {
  name: Image to Image - Single
  type: http
  seq: 1
}

post {
  url: {{HF_BASE_URL}}/hf-inference/models/{{model_image_to_image}}
  body: multipartForm
  auth: bearer
}

body:multipart-form {
  inputs: @file({{sample_png}})
}
