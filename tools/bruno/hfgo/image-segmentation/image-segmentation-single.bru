meta {
  name: Image Segmentation - Single
  type: http
  seq: 1
}

post {
  url: {{HF_BASE_URL}}/hf-inference/models/{{model_image_segmentation}}
  body: multipartForm
  auth: inherit
}

headers {
  Accept: image/png
}

body:multipart-form {
  inputs: @file({{sample_png}})
}
