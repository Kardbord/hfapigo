meta {
  name: Image Classification - Params
  type: http
  seq: 2
}

post {
  url: {{HF_BASE_URL}}/hf-inference/models/{{model_image_classification}}
  body: multipartForm
  auth: inherit
}

headers {
  Accept: application/json
}

body:multipart-form {
  inputs: @file({{sample_png}})
  parameters: {"function_to_apply": "softmax", "top_k": 5}
}
