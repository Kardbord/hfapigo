meta {
  name: Object Detection - Params
  type: http
  seq: 2
}

post {
  url: {{HF_BASE_URL}}/hf-inference/models/{{model_object_detection}}
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  inputs: @file({{sample_png}})
  parameters: {"threshold": 0.6}
}
