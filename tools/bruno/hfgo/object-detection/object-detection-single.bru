meta {
  name: Object Detection - Single
  type: http
  seq: 1
}

post {
  url: {{HF_BASE_URL}}/hf-inference/models/{{model_object_detection}}
  body: multipartForm
  auth: bearer
}

body:multipart-form {
  inputs: @file({{sample_png}})
}
