meta {
  name: Automatic Speech Recognition - Params
  type: http
  seq: 2
}

post {
  url: {{HF_BASE_URL}}/hf-inference/models/{{model_automatic_speech_recognition}}
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  inputs: @file({{sample_audio}})
  parameters: {"return_timestamps": true}
}
