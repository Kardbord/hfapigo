meta {
  name: Image Text to Text - Single
  type: http
  seq: 1
}

post {
  url: {{HF_BASE_URL}}/hf-inference/models/{{model_image_text_to_text}}
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  inputs: @file({{sample_png}})
  text: What is shown in this image?
}
